<div class="grid-sizer"></div>
<div class="grid-item">
  <a href="{% if panelist.redirect %}{{ panelist.redirect }}{% else %}{{ panelist.url | relative_url }}{% endif %}">
    <div class="card hoverable">
      {% if panelist.img %}
        {%
          include figure.liquid
          loading="eager"
          path=panelist.img
          sizes = "250px"
          alt="panelist thumbnail"
        %}
      {% endif %}
      <div class="card-body">
        <h2 class="card-title text-lowercase">{{ panelist.title }}</h2>
        <p class="card-text">{{ panelist.description }}</p>
        <div class="row ml-1 mr-1 p-0">
          {% if panelist.github %}
            <div class="github-icon">
              <div class="icon" data-toggle="tooltip" title="Code Repository">
                <a href="{{ panelist.github }}"><i class="fa-brands fa-github gh-icon"></i></a>
              </div>
              {% if panelist.github_stars %}
                <span class="stars" data-toggle="tooltip" title="GitHub Stars">
                  <i class="fa-solid fa-star"></i>
                  <span id="{{ panelist.github_stars }}-stars"></span>
                </span>
              {% endif %}
            </div>
          {% endif %}
        </div>
      </div>
    </div>
  </a>
</div>
